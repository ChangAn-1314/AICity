/**
 * 闪烁特效动画材质，创建动态闪烁效果。
 *
 * 支持两种闪烁类型：方块闪烁和波纹闪烁。方块闪烁创建简单的明暗交替效果，
 * 波纹闪烁创建从中心向外扩散的波纹效果。可以通过配置动画参数和纹理参数
 * 来实现不同的视觉效果。
 *
 * @category Materials
 * @example
 * ```javascript
 * // 方块闪烁特效动画材质
 * const material = new mapvthree.TwinkleMaterial({
 *     mapSrc: 'website_assets/floor/texture.png',
 *     color: new THREE.Color('#fff'),
 *     opacity: 0.5,
 *     size: [50, 20],
 *     speed: 1,
 *     effectType: mapvthree.TwinkleMaterial.TYPE_BLINK,
 *     emissive: new THREE.Color(0x001155),
 * });
 * ```
 * @example
 * ```javascript
 * // 波纹闪烁特效动画材质
 * const material = new mapvthree.TwinkleMaterial({
 *     mapSrc: 'website_assets/floor/texture.png',
 *     color: new THREE.Color('#fff'),
 *     opacity: 0.5,
 *     size: [50, 20],
 *     spreadSize: 1,
 *     spreadColor: new THREE.Color('#fff'),
 *     speed: 1,
 *     effectType: mapvthree.TwinkleMaterial.TYPE_RIPPLE,
 *     emissive: new THREE.Color(0x001155),
 * });
 * ```
 */
export class TwinkleMaterial extends EffectCommonMaterial {
    /**
     * 方块闪烁类型
     * @type {number}
     */
    static TYPE_BLINK: number;
    /**
     * 波纹闪烁类型
     * @type {number}
     */
    static TYPE_RIPPLE: number;
    /**
     * 构造函数
     * @param {Object} parameters 材质参数
     * @param {number} [parameters.effectType] 闪烁动画类型，可选值有`TwinkleMaterial.TYPE_BLINK`和`TwinkleMaterial.TYPE_RIPPLE`，默认为1
     * @param {number} [parameters.speed] 闪烁动画速度，默认为1
     * @param {Array<number>} [parameters.size] 整体效果尺寸大小，默认为[1, 1]，单位为米
     * @param {string|number|THREE.Color} [parameters.color] 动画效果颜色，默认为0xffffff
     * @param {number} [parameters.opacity] 整体效果透明度，默认为1
     * @param {string|number|THREE.Color} [parameters.emissive] 自发光颜色，默认为0x000000（不发光）
     * @param {string} [parameters.mapSrc] 打底纹理的图片地址，不设置则没有纹理打底
     * @param {string|number|THREE.Color} [parameters.spreadColor] 仅当`effectType`为`TwinkleMaterial.TYPE_RIPPLE`时有效，表示波纹的颜色，默认为0xffffff
     * @param {number} [parameters.spreadSize] 仅当`effectType`为`TwinkleMaterial.TYPE_RIPPLE`时有效，表示波纹的扩散大小，默认为1
     */
    constructor(parameters: {
        effectType?: number | undefined;
        speed?: number | undefined;
        size?: number[] | undefined;
        color?: string | number | THREE.Color;
        opacity?: number | undefined;
        emissive?: string | number | THREE.Color;
        mapSrc?: string | undefined;
        spreadColor?: string | number | THREE.Color;
        spreadSize?: number | undefined;
    });
    name: string;
    transparent: boolean;
    fragmentShader: any;
    vertexShader: any;
}
import { EffectCommonMaterial } from './EffectCommonMaterial';
