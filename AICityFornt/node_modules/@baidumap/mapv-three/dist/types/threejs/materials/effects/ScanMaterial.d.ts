/**
 * 扫描特效动画材质，创建一个动态扫描线效果。
 *
 * 扫描特效由扫描动画和可选的纹理动画组成，支持垂直和水平两种扫描方向。
 * 可以通过配置动画参数和纹理参数来实现不同的视觉效果，如雷达扫描、进度条等。
 *
 * @category Materials
 * @example
 * ```javascript
 * // 普通扫描特效动画材质
 * const material = new mapvthree.ScanMaterial({
 *     color: new THREE.Color(0x66aadd),
 *     size: [100, 40],
 *     speed: 500,
 *     direction: mapvthree.ScanMaterial.DIRECTION_VERTICAL,
 *     emissive: new THREE.Color(0x001144),
 * });
 * ```
 * @example
 * ```javascript
 * // 带纹理打底的扫描特效动画材质
 * const material = new mapvthree.ScanMaterial({
 *     mapSrc: 'website_assets/floor/radar.png',
 *     mapSize: [5, 10],
 *     mapSpeed: 40,
 *     color: new THREE.Color('#fff'),
 *     size: [36, 36],
 *     sizeRatio: 0.7,
 *     speed: 80,
 *     direction: mapvthree.ScanMaterial.DIRECTION_VERTICAL,
 *     emissive: new THREE.Color(0x002288),
 * });
 * ```
 * @example
 * ```javascript
 * // 带纹理遮罩的扫描特效动画材质
 * const material = new mapvthree.ScanMaterial({
 *     maskMap: new THREE.TextureLoader().load('website_assets/floor/mask.png'),
 *     color: new THREE.Color('#fff'),
 *     size: [1, 0.05],
 *     speed: 0.1,
 *     emissive: new THREE.Color(0x001155),
 * });
 * ```
 */
export class ScanMaterial extends EffectCommonMaterial {
    /**
     * 垂直扫描方向
     * @type {number}
     */
    static DIRECTION_VERTICAL: number;
    /**
     * 水平扫描方向
     * @type {number}
     */
    static DIRECTION_HORIZONTAL: number;
    /**
     * 构造函数
     * @param {Object} parameters 材质参数
     * @param {number} [parameters.direction] 扫描动画方向，可选值有`ScanMaterial.DIRECTION_VERTICAL`和`ScanMaterial.DIRECTION_HORIZONTAL`，默认为1
     * @param {number} [parameters.speed] 扫描动画速度，默认为1
     * @param {Array<number>} [parameters.size] 整体效果尺寸大小，默认为[1, 1]，单位为米
     * @param {number} [parameters.sizeRatio] 扫描动画部分占整体的比例值，默认为1（填满），0.5表示特效占整体的1/2
     * @param {string|number|THREE.Color} [parameters.color] 动画效果颜色，默认为0xffffff
     * @param {number} [parameters.opacity] 整体效果透明度，默认为1
     * @param {string|number|THREE.Color} [parameters.emissive] 自发光颜色，默认为0x000000（不发光）
     * @param {string} [parameters.mapSrc] 纹理动画的纹理图片地址，不设置则没有纹理动画
     * @param {Array<number>} [parameters.mapColor] 纹理动画的颜色，默认为[1, 1, 1]
     * @param {number} [parameters.mapSpeed] 纹理动画的速度，默认为1
     * @param {Array<number>} [parameters.mapSize] 纹理动画的尺寸，默认为[1, 1]，单位为米
     * @param {THREE.Texture} [parameters.maskMap] 动画的遮罩图层，透明部分不显示，不透明部分显示，不设置则没有遮罩效果
     */
    constructor(parameters: {
        direction?: number | undefined;
        speed?: number | undefined;
        size?: number[] | undefined;
        sizeRatio?: number | undefined;
        color?: string | number | THREE.Color;
        opacity?: number | undefined;
        emissive?: string | number | THREE.Color;
        mapSrc?: string | undefined;
        mapColor?: number[] | undefined;
        mapSpeed?: number | undefined;
        mapSize?: number[] | undefined;
        maskMap?: any;
    });
    name: string;
    transparent: boolean;
    fragmentShader: any;
    vertexShader: any;
}
import { EffectCommonMaterial } from './EffectCommonMaterial';
