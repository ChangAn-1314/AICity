/**
 * 萤火虫粒子效果，创建跟随相机的萤火虫群效果。
 *
 * 萤火虫粒子会在相机周围随机分布，并具有自然的运动效果。
 * 粒子系统会自动跟随相机位置，确保萤火虫始终在视野范围内。
 *
 * 注意：需要在引擎中启用动画循环 `rendering.enableAnimationLoop=true`
 *
 * @category Natural
 * @example
 * ```javascript
 * // 创建萤火虫效果
 * const fireflies = engine.add(new mapvthree.Fireflies({
 *     color: new THREE.Color(0x0099aa),
 *     size: 2,
 *     countFactor: 0.6
 * }));
 * ```
 */
export class Fireflies {
    /**
     * 构造函数
     * @param {Object} options 配置参数
     * @param {string|THREE.Color} [options.color] 萤火虫颜色，支持十六进制颜色值或THREE.Color对象，默认为0x0099aa
     * @param {number} [options.size] 萤火虫大小，默认为1
     * @param {number} [options.countFactor] 萤火虫数量因子，范围0~1，控制活跃粒子数量，默认为0.5
     */
    constructor(options?: {
        color?: string | THREE.Color;
        size?: number | undefined;
        countFactor?: number | undefined;
    });
    /**
     * 粒子数量的比例因子
     * @private
     */
    private _countFactor;
    /** @private */ private _clock;
    /** @private */ private _color;
    /** @private */ private _engine;
    /** @private */ private _emitter;
    /** @private */ private _system;
    /** @private */ private _options;
    /**
     * 添加到引擎后的初始化
     * @param {Engine} engine 引擎实例
     * @internal
     */
    afterAddToEngine(engine: Engine): void;
    /**
     * 从引擎移除前的清理
     * @internal
     */
    beforeRemoveFromEngine(): void;
    /**
     * 为材质添加自发光属性
     * @param {Object} material 材质对象
     * @internal
     */
    overrideMaterialWithEmissive(material: Object): void;
    /**
     * 渲染前的处理函数，更新萤火虫位置跟随相机
     * @param {Engine} engine 引擎实例
     * @internal
     */
    handleBeforeRender: (engine: Engine) => void;
    /**
     * 更新粒子系统
     * @internal
     */
    update: () => void;
    /**
     * 创建粒子纹理
     * @returns {THREE.Texture} 粒子纹理
     * @internal
     */
    createSnow: () => THREE.Texture;
    /**
     * 创建粒子发射器
     * @returns {Emitter} 粒子发射器
     * @internal
     */
    createEmitter: () => Emitter;
    /**
     * 设置萤火虫数量因子
     * @param {number} value 数量因子，范围0~1
     */
    set countFactor(value: number);
    /**
     * 萤火虫数量因子
     * @type {number}
     */
    get countFactor(): number;
    /**
     * 释放资源
     * 清理纹理和粒子系统
     */
    dispose(): void;
}
